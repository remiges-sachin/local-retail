routingRules:
  - version: "2.0.0"
    targetType: "url"
    target:
      url: "https://bppapp.remiges.tech/api/webhook"
    endpoints:
      - select
      - init
      - confirm
      - status
      - cancel
      - support
